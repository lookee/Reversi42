name: Performance Benchmarks

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    # Run nightly at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  benchmark:
    name: Run Performance Benchmarks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
    
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-benchmark memory-profiler
    
    - name: Run bitboard benchmarks
      run: |
        python -c "
        import sys
        import os
        sys.path.insert(0, os.path.join(os.getcwd(), 'src'))
        
        import time
        from copy import deepcopy
        from Reversi.BitboardGame import BitboardGame
        from Reversi.Game import Move
        
        # Benchmark move generation
        game = BitboardGame()
        iterations = 10000
        
        start = time.perf_counter()
        for _ in range(iterations):
            moves = game.get_valid_moves()
        elapsed = (time.perf_counter() - start) / iterations * 1e9
        
        result = f'Move generation: {elapsed:.1f} ns/op\n'
        print(result, end='')
        
        # Benchmark make_move (using D3 = Move(4, 3))
        test_move = Move(4, 3)
        start = time.perf_counter()
        for _ in range(iterations):
            test_game = deepcopy(game)
            if test_game.valid_move(test_move):
                test_game.move(test_move)
        elapsed = (time.perf_counter() - start) / iterations * 1e9
        
        result += f'Make move: {elapsed:.1f} ns/op\n'
        print(f'Make move: {elapsed:.1f} ns/op')
        
        # Save to file
        with open('benchmark_bitboard.txt', 'w') as f:
            f.write(result)
        "
    
    - name: Run AI performance benchmarks
      run: |
        python -c "
        import sys
        import os
        sys.path.insert(0, os.path.join(os.getcwd(), 'src'))
        
        import time
        from Reversi.BitboardGame import BitboardGame
        from Players.PlayerApocalyptron import PlayerApocalyptron
        
        game = BitboardGame()
        player = PlayerApocalyptron(depth=6, book_instant=True)
        
        # Benchmark AI move at depth 6
        moves = game.get_move_list()
        
        start = time.perf_counter()
        move = player.get_move(game, moves, None)
        elapsed = time.perf_counter() - start
        
        result = f'AI move (depth 6): {elapsed:.3f} seconds\n'
        print(result, end='')
        
        # Should be under 1.0 seconds (CI-friendly threshold)
        if elapsed > 1.0:
            result += '⚠️  Warning: AI slower than expected\n'
            print('⚠️  Warning: AI slower than expected')
        else:
            result += '✅ AI performance within limits\n'
            print('✅ AI performance within limits')
        
        # Save to file
        with open('benchmark_ai.txt', 'w') as f:
            f.write(result)
        "
      timeout-minutes: 10
      env:
        REVERSI42_VIEW: headless
    
    - name: Memory usage test
      run: |
        python -c "
        import sys
        import os
        sys.path.insert(0, os.path.join(os.getcwd(), 'src'))
        
        from Reversi.BitboardGame import BitboardGame
        
        # Check game state size
        game = BitboardGame()
        size = sys.getsizeof(game)
        
        result = f'BitboardGame size: {size} bytes\n'
        print(result, end='')
        
        # Should be under 1KB
        if size > 1024:
            result += '⚠️  Warning: Game state larger than expected\n'
            print('⚠️  Warning: Game state larger than expected')
        else:
            result += '✅ Memory usage optimal\n'
            print('✅ Memory usage optimal')
        
        # Save to file
        with open('benchmark_memory.txt', 'w') as f:
            f.write(result)
        "
    
    - name: Upload benchmark results
      uses: actions/upload-artifact@v6
      with:
        name: benchmark-results
        path: |
          benchmark_bitboard.txt
          benchmark_ai.txt
          benchmark_memory.txt
        retention-days: 90
      if: always()
    
    - name: Comment PR with results
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v8
      with:
        script: |
          const output = 'Benchmark results will be added here';
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `## 📊 Performance Benchmark Results\n\n${output}`
          });
      continue-on-error: true

